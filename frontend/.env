# Vite dev proxy target (optional). The UI calls /api/* and Vite proxies it.
VITE_BACKEND_URL=http://localhost:5000

# Express production server proxy target (required for `npm start`)
BACKEND_URL=https://your-backend-domain.up.railway.app


