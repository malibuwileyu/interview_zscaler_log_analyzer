services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: log_analyzer
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - log_analyzer_pgdata:/var/lib/postgresql/data

volumes:
  log_analyzer_pgdata:


